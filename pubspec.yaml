name: tic_tac_showdown

description: This Is Flutter TIC TAC TOE App BY HHS Tech

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  google_mobile_ads: ^4.0.0
  clean: ^0.1.0
  flutter_clean: ^0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true