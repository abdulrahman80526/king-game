name: tic_tac_showdown
description: "A sleek Tic Tac Toe game with smart AI and AdMob integration."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_mobile_ads: ^5.1.0 # Or your currently cached version

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Ensure your asset folder path is registered here
  assets:
    - assets/icon/